--- a/webview/assets/app-initial~app-main~onboarding-page-CNHnOMz8.js
+++ b/webview/assets/app-initial~app-main~onboarding-page-CNHnOMz8.js
@@ -135163,8 +135163,18 @@
     d = new EventTarget(),
     f = new Dnt(),
     p = o,
+    inputModeDisabled = !1,
+    getAttributes = () =>
+      inputModeDisabled
+        ? { spellcheck: `true` }
+        : { spellcheck: `true`, inputmode: `none` },
+    setPointerInputMode = (e) => {
+      inputModeDisabled !== e &&
+        ((inputModeDisabled = e),
+        m.isDestroyed || m.setProps({ attributes: getAttributes }));
+    },
     m = new IF(null, {
-      attributes: { spellcheck: `true` },
+      attributes: getAttributes,
       state: V8e.create({
         schema: lI,
         doc:
@@ -135241,6 +135251,17 @@
         let t = m.state.apply(e);
         (m.updateState(t), d.dispatchEvent(new Event(TN)));
       },
+      handleDOMEvents: {
+        mousedown(e, t) {
+          return (setPointerInputMode(!0), !1);
+        },
+        touchstart(e, t) {
+          return (setPointerInputMode(!0), !1);
+        },
+        blur(e, t) {
+          return (setPointerInputMode(!1), !1);
+        },
+      },
       handlePaste(e, t) {
         if (t.defaultPrevented) return !0;
         let n = t.clipboardData,
