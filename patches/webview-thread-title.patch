--- a/webview/assets/local-conversation-thread-DhV7q0nP.js
+++ b/webview/assets/local-conversation-thread-DhV7q0nP.js
@@ -453,6 +453,7 @@
   po as Ro,
   pr as zo,
   pv as Bo,
+  qr as codexHostedWindowTitleAtom,
   qn as Vo,
   rc as Ho,
   rr as Uo,
@@ -17098,6 +17099,7 @@
     x = F(mo, e) ?? !1,
     S = F(Jo, e),
     C = F(Ea, e),
+    codexHostedWindowTitle = F(codexHostedWindowTitleAtom, e),
     w = F(po, e) === `projectless`,
     T = F($o, e),
     E = F(Zo, e),
@@ -17124,6 +17126,10 @@
   (0, GS.useEffect)(() => {
     U?.id == null || U.readAt != null || H(U.id);
   }, [U?.id, U?.readAt, H]);
+  (0, GS.useEffect)(() => {
+    let t = codexHostedWindowTitle?.trim();
+    t && (document.title = `${t} | Codex`);
+  }, [codexHostedWindowTitle]);
   let re = es(),
     { agentMode: ie, shouldSendPermissionOverrides: ae } = Bt({
       conversationId: e,
